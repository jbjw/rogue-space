//

"use strict"

export default class Ship {
	constructor( args ) {
		args
		// args.blueprint
		// args.name
		// args.shield
		// args.guns
		// this.components = []
	}
}

Ship.prototype = {
	// addVertex: function(v) {
	// 	this.vertices.push(v)
	// },
}
